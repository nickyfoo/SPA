procedure Test {
    read y;
    x = y;
    sum = sum * sum + f + x;
    z = 1;
    f = 0;
    y = 1;
    count = count + 1;
    sum = 0;
    call Example;
    while (count <= x) {
        sum = sum + y;
        m = y + z;
        y = z;
        z = m;
        if (sum > 10) then {
            f = 1;
        } else {
            f = 0;
        }
        count = count + 1;
    }
    print sum;
    if (count == 4) then {
        x = x + 3;
    } else {
        y = y + 1;
    }
    a = f + y + z + sum * sum / f + x;
    print a;
}

procedure Example {
  x = 1 + 2 + 3;
  z = 3;
  i = 5;
  while (i!=0) {
    x = x - 1;
    if (x==1) then {
      z = x + 1; }
    else {
      y = z + x; }
    z = z + x + i;
    read x;
    i = i - 1; }
  print z; }